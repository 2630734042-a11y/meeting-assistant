# Docker Compose 部署配置
# 启动: docker-compose up -d
#
# 你需要定义以下服务:

version: '3.8'

# TODO: 定义 services
#   1. meeting-assistant 服务:
#      - build: context=. (需要同目录的 Dockerfile)
#      - ports: "8000:8000"
#      - env_file: .env (加载环境变量)
#      - volumes: ./src:/app/src (代码热更新)
#      - restart: unless-stopped
#
#   2. postgres 服务 (可选):
#      - image: postgres:16-alpine
#      - environment: POSTGRES_DB/POSTGRES_USER/POSTGRES_PASSWORD
#      - ports: "5432:5432"
#
#   3. redis 服务 (可选):
#      - image: redis:7-alpine
#      - ports: "6379:6379"
#
# TODO: 定义 volumes (pgdata, redisdata)
